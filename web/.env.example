# Frontend env example
# 复制为 .env.local 并填入实际值（Next.js 自动读 .env.local，不入库）

# ===== 后端 API 地址 =====
# 本地 dev 默认就行（跟 .env 里的 COCKPIT_PORT 对齐，默认 7842）
# 部署到远程/容器时才需要改
NEXT_PUBLIC_API_BASE=http://127.0.0.1:7842
