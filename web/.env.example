# Web 端配置示例
# 复制为 .env.local 生效

# 后端 API 地址
# 默认空字符串 = 走 next.config.mjs 的 rewrites 代理到 127.0.0.1:7842
# ⚠️ Next.js dev 模式 proxy 默认 30s timeout，超时返 500。
#    LLM 调 5+ tool call 通常 > 30s，建议直连后端。
NEXT_PUBLIC_API_BASE=http://localhost:7842
