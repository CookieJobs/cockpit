# Production build env (Docker 部署用)
#
# 部署时 (静态导出 + FastAPI 同源服务) 前端 fetch 用相对路径 /api/*,
# 浏览器自动用当前 origin 拼绝对地址, 单端口方案最干净。
# 这样 IP 访问 (http://62.234.180.241:7842) 或域名访问 (https://cockpit.example.com)
# 都不用改这里。
NEXT_PUBLIC_API_BASE=/api
