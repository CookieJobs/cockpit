# Production build env (Docker 部署用)
#
# 部署时 (静态导出 + FastAPI 同源服务) fetch 走相对路径,
# 浏览器自动用当前 origin 拼绝对地址. lib/api.ts 里的 path 已经
# 自带 /api/... 前缀 (如 "/api/health"), 所以这里设为空字符串.
# 部署在任意 IP/域名都不用改这个文件.
#
# 重要: 这个值会被 build 时 inline 到 JS bundle, Dockerfile 里也通过
# ARG NEXT_PUBLIC_API_BASE="" 显式注入 (覆盖本地 .env.local).
NEXT_PUBLIC_API_BASE=
